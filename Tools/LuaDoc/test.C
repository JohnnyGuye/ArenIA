/**lua function_name( arg1:string, arg2:string ):int,:int
	@desc Cette fonction sert à manger des pommes
	@param arg1:string Un string à uttiliser comme stroumph
	@param arg2:string Un poulet au fromage
	@return An int for the position x
	@return An int for the position y
	@exemple Nom de l'example
	function_name()
	if a = b then
		-- This is some comment
		print("Yay")
	end
	-- Note : Another comment
	!eend
	
*/
static void test()
{
	//do stuff
}


/**lua function_name2(arg1:string, arg2:string, arg3:int):void
	@desc Cette fonction ne sert a rien, mais elle a un long truc de texte avec elle ce qui fait qu'en fait c'est une fonction qui ne veut rien dire et parfois j'aime bien manger des herissons à la broche, c'est bon les hérissons à la broche yay
	@param arg1 Do stuff
	@param arg2 Do otherStuff
	@exemple This is another exemple !
	function main()
		move(5)
		while(true) do
			-- Wait 'till the end of the world
		end
	end
	!eend
*/
static void test()
{
	//do stuff
}
